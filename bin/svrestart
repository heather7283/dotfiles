#!/bin/sh

if [ "$1" = '-u' ]; then
  livedir="${XDG_RUNTIME_DIR}/s6/live"
  shift
else
  livedir=/run/s6-rc/
fi

if [ -n "$1" ]; then
  s6-rc -l "$livedir" stop "$@" && s6-rc -l "$livedir" start "$@"
fi

