#!/bin/bash

new_args=()
for arg in "$@"; do
  if [[ $arg != "--ansi" ]]; then
    new_args+=("$arg")
  fi
done

if [ -f ~/.config/fzf/flags ]; then
  default_args="$(tr '\n' ' ' <~/.config/fzf/flags)"
fi

exec /usr/bin/fzf $default_args "${new_args[@]}"

